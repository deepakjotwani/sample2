---
name: Test

on:
  pull_request:
    branches:
      - "**"
  workflow_run:
    workflows: ["Dependabot PR Workflow"]
    branches:
      - 'dependabot/**'
    types:
      - requested    
jobs:
  master:
    runs-on: ubuntu-latest
    steps:
      - name: echo-secret
        run: echo $USERNAME
    env:
        USERNAME: ${{ secrets.DOCKER_USERNAME }}
